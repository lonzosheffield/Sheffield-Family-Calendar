VERDICT: FAIL

# QA — Homeschool ("School") wave, round 1

**Auditor:** Fable 5 (HS8, fresh context, no prior knowledge of the run) · **Date:** 2026-09-03 ·
**Tree:** `main` @ `c7c1339`, audited as the full diff `abb202b..HEAD` (71 files, +17,834 / −88) ·
**Contract:** `docs/homeschool/PLAN_HOMESCHOOL.md` v3.1 §0 (N1), §2 H1–H8 (normative SQL, occurrence
rule, signatures, DTOs), §3 HS1–HS7 Owns/Do/Accept, §4 defaults; reviews `RED_HS`, `PURPLE_HS`,
`WHITE_HS`, `DELTA_V3`; `docs/PLAN.md` §5; `docs/design/DESIGN_DIRECTION.md` §2–§3; `docs/PROTOCOL.md`.

Verdict is **FAIL** on two High and three Med findings (QH1-01…QH1-05). Everything the committed
acceptance suites can see is green — the defects below are in the paths the suites do not reach:
a second write to the same log row, the live reactivity of the phone's Year/Month panes, and the
"done" lifecycle of a parent-added task on both surfaces.

## What was run on this machine (all exit 0 unless stated)

| Gate | Result |
| --- | --- |
| `cargo test --features server` (run 1) | 33 `test result:` lines, **606 passed, 0 failed**, 2 ignored (pre-existing) |
| `cargo test --features server` (run 2, consecutive) | 33 `test result:` lines, **606 passed, 0 failed**, 2 ignored — identical to run 1 (HS7 (d)) |
| `cargo clippy --features server --all-targets -- -D warnings` | exit 0 |
| `cargo clippy --features web --target wasm32-unknown-unknown -- -D warnings` | exit 0 (the `--all-targets` form HS3 (e) names cannot build on wasm — pre-existing `mio` dev-dep, HANDOFF H-HS3-3; see QH1-09) |
| `cargo fmt --check` | exit 0 |
| `git ls-files docs/homeschool/curriculum/` | empty |
| `git grep -i ambleside` | `.gitignore`, the plan, the four reviews, `docs/HANDOFF.md` (one line naming the guard), `docs/VERIFICATION.md` (see QH1-06) — **nothing under `src/`, `tests/`, `assets/`** |
| Every `name`/`source`/`text` string ≥ 6 chars from the gitignored `ao-year-1.toml` grepped over every tracked text file | hits only on generic plan vocabulary (`Poetry`, `Geography`, `Recitation`, `Free reads`, `Composer`), the curriculum's display name and six subject labels in `docs/VERIFICATION.md` (QH1-06), and two generic parser examples in `tests/curriculum_tests.rs`. **No passage, book title or week→reading string is tracked.** `docs/verification/tv-school-week1-2026-09-03.jpg` inspected: subject names only. |

### Accept clauses executed by hand (beyond the two full runs)

1. **HS7 (a)(b)** — `cargo test --test homeschool_loop_tests`: TV-side `toggle_lesson` → phone < 1 s, phone-authed `set_school_week` → TV < 1 s, Together tick → both, kill/restart → both reconnect and every pre-restart write survives. `1 passed`.
2. **HS5 (a)(b)(c)(d)(e)(g)(h)(i)(j)(k)** — `cargo test --test glyph_tests hs5_` (17 SSR/queue tests): Today under `SignedOut` vs `Parent`, six-tab bar + pixel budget, Year grid (6 subject rows + header, `Twice Told` ×2 on Tuesday, `min-h-[44px]`, `overflow-x-auto`), Month (30 cells, 📌, `n/m`, bare count, `data-month-weekend`), day sheet, queue replay. `17 passed`.
3. **HS6 (a)–(h)** — `cargo test --test tv_tests hs6_`: golden section, BFS ≤ 12 presses (worst case 8), tick-every-row-with-the-remote, `SetView(Homeschool)`, type-scale/overscan, 4-panel wrap, no shared row, 📌 extra focusable/activatable. `9 passed`.
4. **HS4 (a)–(m)** — `cargo test --test homeschool_tests`: `16 passed`.
5. **HS1 (a)–(i)** — `cargo test --test homeschool_db_tests`: `26 passed`, N1 guard included.
6. **HS1 (g)** by hand with `target\debug\family-hub.exe import-curriculum` against a scratch `FAMILY_HUB_DATA_DIR`: missing path → exit 1, nothing copied, no `family.db`; `days = "Th"` file → exit 1 with `bad.toml: line 6: … 'h' is not one of the day letters MTWRFSU`, nothing copied, no `family.db`; the committed fixture → exit 0, copied into `curricula\`.
7. **HS3 (a)–(c)(f)–(i)** — `cargo test --lib hs3_`: `34 passed`. **HS3 (d)** — `realtime_tests` protocol-doc test + `hs3_the_server_message_sample_vector_gained_exactly_the_two_homeschool_variants`: `3 passed`.
8. **HS2 (a)–(h)** — `cargo test --test curriculum_tests` with the gitignored files present: `9 passed` (the suite prints `SKIP` with a reason when they are absent — verified by reading `load()`).

### Contract items checked by reading, all satisfied

`migrations/0005_homeschool.sql` is H1 verbatim (expression unique index, `IFNULL`, both CHECKs). H3
rules 1–10 in `src/shared/homeschool.rs` match the normative text including the rule-5 chunking (2×3 →
Mon+Wed / Fri, 1×2 → parts 1/2, 2×1 → both Tuesday, 0 rows → none); `parse_days` rejects `Th`/`MM`/`X`;
Sakamoto `weekday` gives the four pinned answers; no date crate in `src/shared/` (one pre-existing doc
comment mentions the word). H4 Together fan-out is one transaction with per-member revalidation and
names every matched boy. H5 loader is insert-missing-only keyed `(slug, subject, week, ordinal)`,
validation ordered and all-or-nothing with file name + `line N`; `--replace` updates in place and counts
orphaned log rows; `curricula_dir()` is a method, absolute, created at boot, logged at `info`. H7: every
`auth` fn calls `require_session_or_cookie` before touching the pool; `toggle_lesson` recomputes the
boy's current-week occurrences and rejects any other `(subject, assignment, scheduled_date)`, rejects
`week ≠ current_week`, `subject_id ≤ 0` before the date window, and the ±1 day `date` window applies to
every dated mutation including `toggle_extra`; `add_extra` bounds `scheduled_date` to ±365 days. H6
realtime: `homeschool_version` bumped by both variants and by `bump_all` (Resync/reconnect); the TV
resource is keyed on `homeschool_version` **and** `bus.today`, so midnight and reconnect both refetch;
`FocusId::Lesson(String)`/`Extra(i64)` keyed on identity, not index. Idempotency: every dated mutation
claims its key inside the same transaction as its write. Design direction: local fonts only (no external
URL in any new file), every ground/ink pair on both surfaces is already in `PALETTE_PAIRS` (palette suite
walks all four TV panels and `components/**`), TV type scale golden unchanged, overscan walker green, glyphs
are emoji constants in `glyphs.rs` (`🏠 📖 ✏️ 🎨 📚 📌 🎉`), TV section labels are the §2.6 tracked-caps
device on `TV_BODY_TEXT`, phone headings are the existing `text-lg font-bold text-sheffield-dark`. No
test weakened: the migration-version/key-count bumps, the five→six tab rename (HS5's own Do clause) and
the `nav.rs`/`tv_tests.rs` panel-count bumps are the §3 mechanical class and are recorded in HANDOFF.
`Cargo.toml`/`Cargo.lock` untouched since `abb202b` (`toml =1.1.5`, server-only). No non-Rust added
(one screenshot JPEG under `docs/verification/`). `docs_tests` id list gained HS1–HS7 in the HS7 commit;
link checker green with `docs/homeschool/**` in scope.

## Findings

Tier = the tier of the agent originally assigned (§3 roster: HS1 HS3 HS5 HS6 = O; HS2a HS2b HS4 HS7 = S).
Severity: Critical / High / Med / Low. PASS requires zero Critical/High/Med.

| id | task | tier | file:line | severity | description | solution (apply verbatim) |
| --- | --- | --- | --- | --- | --- | --- |
| QH1-01 | HS4 | S | `src/server/api/homeschool.rs:775-778` | **High** | `toggle_lesson` with `completed = true` only calls `hs::set_occurrence`, which is `INSERT … ON CONFLICT DO NOTHING` (H1). On an occurrence that already has a log row the call is therefore a silent no-op: a parent's **Skip** on a ticked row (`today.rs` `on_skip` sends `completed: true, status: Skipped`) leaves it `done`, and a **Note** on any ticked row (`on_note` sends `completed: true` + the note) is dropped — W-6's "stopped at p.40" never reaches the database and the phone shows no error. HANDOFF HS-4 told HS4 in as many words that "skipping something already ticked is `clear_occurrence` then `set_occurrence`, in the server fn's existing transaction"; it was not implemented. `hs4_*` never writes the same key twice with a different status/note, so the suite is green. | In `toggle_lesson`, replace the `if completed { … } else { … }` block inside `if claimed` with: `if completed { hs::clear_occurrence(&mut *tx, &key).await.map_err(super::to_server_error)?; hs::set_occurrence(&mut *tx, &key, status.as_str(), note.as_deref(), &date).await.map_err(super::to_server_error)?; } else { hs::clear_occurrence(&mut *tx, &key).await.map_err(super::to_server_error)?; }` (same transaction, still idempotent under the claim; `mark_all_done` is unaffected because it targets `status.is_none()` only). Add `hs4_n_a_skip_or_a_note_on_an_already_ticked_row_replaces_its_state` to `tests/homeschool_tests.rs`: tick an occurrence `Done`; call again (new key) with `status: Skipped` → `SELECT status` = `skipped`, row count 1; call again with `Done` and `note: Some("p.40")` → `note` = `p.40`, row count 1. |
| QH1-02 | HS5 | O | `src/client/components/homeschool/mod.rs:304-348` | **High** | The Year/Month/subject resources capture `focus`, `grid_week`, `cursor` and `settings_curriculum` as plain values computed *outside* their `use_resource` closures (lines 304, 311–312, 314, 339–341). Dioxus 0.7.10's `use_resource` reruns only when a signal read **inside** the closure changes (`ReactiveContext`, `dioxus-hooks/src/use_resource.rs`); a component re-render does not restart it. So tapping a week in the Year picker (`selected_week`), stepping the month (`month_cursor`) or picking a boy chip (`boy_filter`) re-renders the tab but never refetches — the grid and the month stay on whatever was first fetched until an unrelated `homeschool_version` bump or a mutation's `.restart()`. H6's "week picker 1…36" and "Month view … ←/→" therefore do not work in the live app. The SSR acceptance tests render `YearPanel`/`MonthPanel` with props directly, so they cannot see it, and HS7's Chrome pass could not reach `/m` (CA interstitial). | Make the derived values reactive and read them inside the closures. Replace lines 304–312 with: `let enrollments_memo = use_memo(move \|\| match &*enrollments_res.read() { Some(Ok(rows)) => rows.clone(), _ => Vec::new() }); let focus = use_memo(move \|\| focused_boy(boy_filter(), &enrollments_memo())); let enrollment = use_memo(move \|\| focus().and_then(\|id\| enrollments_memo().into_iter().find(\|row\| row.user_id == id && row.enrolled))); let current_week = use_memo(move \|\| enrollment().map(\|row\| row.current_week).unwrap_or(1)); let grid_week = use_memo(move \|\| selected_week().unwrap_or(current_week()));` and lines 339–341 with `let cursor = use_memo(move \|\| month_cursor().or_else(\|\| RoutineDateState::resolve((bus.today)(), today_fetch.read().clone()).date().and_then(year_month_of)).unwrap_or((2026, 1)));`. Inside `subjects_res` read `enrollment().map(\|row\| row.curriculum_id)`, inside `grid_res` read `focus()` and `grid_week()`, inside `month_res` read `focus()` and `cursor()` (memo reads are tracked). Use `focus()`, `enrollment()`, `current_week()`, `cursor()` at the later call sites. Add a source-shape guard to `tests/glyph_tests.rs` (same style as `tv_tests::tv_sources`): read `src/client/components/homeschool/mod.rs` and assert each of `grid_week()`, `cursor()`, `focus()` appears **inside** the text between `let mut grid_res = use_resource(` / `let mut month_res = use_resource(` and the next `});`, so a future edit that hoists them out fails a test. |
| QH1-03 | HS3 | O | `src/shared/homeschool.rs:829-839` | **Med** | `merge_extras` tests `extra.status.is_some()` **before** `scheduled_date == date`, so a ticked extra leaves `due_today` and lives only in `done`. H3 rule 10 orders the checks `scheduled_date == date → due_today` first, then catch-up, then `status set → done`, and `today_view` (rule 8) keeps a ticked lesson in `due_today` with its status. Consequence on both surfaces: a parent-added task **vanishes** from the boy's Today block and from the TV list the moment it is ticked (`BoyBlock` and `school_rows` render `due_today + catch_up` only), so a mis-tick cannot be undone there — a boy on the TV has no way back, and the cursor lands on whichever row slid into the slot (the D-2 hazard). `day_items()` in `client/components/homeschool/mod.rs:221-229` already works around it for the day sheet, which is the tell. | Mirror `today_view`: replace lines 829–839 with `if extra.status.is_some() { today.done.push(DayItem::Extra(extra.clone())); } if extra.scheduled_date == date { today.due_today.push(DayItem::Extra(extra.clone())); } else if extra.status.is_none() && extra.scheduled_date.as_str() < date && earliest.as_deref().is_some_and(\|floor\| extra.scheduled_date.as_str() >= floor) { today.catch_up.push(DayItem::Extra(extra.clone())); }`. Delete the `boy.done` pull-back in `day_items()` (mod.rs lines 221–229) so the sheet does not list a ticked extra twice. Extend `hs3_i_merge_extras_files_each_task_by_its_date_and_counts_only_the_current_span`: a `Done` extra dated `date` is in **both** `due_today` and `done` and counts once in `done_count`/`total_count`. The TV fixture (`canonical_school`) and HS6's 12-row budget are unaffected. |
| QH1-04 | HS6 | O | `src/client/components/tv/model.rs:558-565` (and `api/homeschool.rs:433-435`) | **Med** | `TvSchoolState::AllDone` is returned only when `due_today` **and** `catch_up` are both empty. Because `today_view` keeps ticked occurrences in `due_today`, on any school day that dealt out work the H6 "all done → the routine's 8/8-style celebration chip" never renders — the boy just sees a list of ticked rows. The one situation that *does* reach `AllDone` is a boy with no rows at all, e.g. a boy whose own enrollment is paused inside a two-boy group (the group's `paused` is `all(...)`, so H2's "School's out" card is not shown either): he gets **"0 / 0 School work all done!"** instead of "No school today". No `hs6_*` test covers a fully ticked day or a paused member of a group. | In `model::school()`: after computing `due_today`/`catch_up`, `if due_today.is_empty() && catch_up.is_empty() { return TvSchool { week, state: if boy.total_count == 0 { TvSchoolState::NoSchoolToday } else { TvSchoolState::AllDone { done: boy.done_count, total: boy.total_count } } }; }`; add `celebrate: bool` to `TvSchoolState::Day`, set to `catch_up.is_empty() && due_today.iter().all(\|item\| match item { DayItem::Lesson(l) => l.status.is_some(), DayItem::Extra(e) => e.status.is_some() })`; in `surface::homeschool_panel`'s `Day` arm render the existing `tv-school-count` chip row (the `AllDone` markup) **above** the `ul` when `celebrate`, keeping every row focusable so a mis-tick is undoable. Add `hs6_i_a_fully_ticked_day_celebrates_and_a_paused_boy_in_a_group_gets_no_school_today`: fixture with every row `Some(Done)` renders `tv-school-count` and still 12 focusable rows; a `BoyToday` with empty lists and `total_count = 0` renders `No school today`. Phone side (`today.rs:445`): when `boy.total_count == 0 && due_today.is_empty() && catch_up.is_empty()` render "School's out for {boy.name} ⚽" instead of "Nothing left for {boy.name} today." |
| QH1-05 | HS5 | O | `src/client/components/homeschool/today.rs:156` / `mod.rs:596-642` | **Med** | The boy chips exist only inside `TodayPanel`. H6: "A boy chip filters Year exactly as it filters Today. Month view always shows exactly one boy: with more than one enrolled, the first enrolled boy is preselected and **the chip is a required selector** (D-4)." Neither `YearPanel` nor `MonthPanel` renders a chip, so with two boys enrolled the only way to see the second boy's Year or Month is Today → tap his chip → toggle back. Dormant while only Isaiah is enrolled (§5 Q1), live the day Nathaniel is. | Move the chip row into its own component in `today.rs`: `#[component] pub fn BoyChips(boys: Vec<(i64, String)>, selected: Option<i64>, allow_everyone: bool, on_select: EventHandler<Option<i64>>) -> Element` (same markup as lines 157–171; render the `Everyone` button only when `allow_everyone`). `TodayPanel` calls it with `allow_everyone: true`. Give `YearPanel` and `MonthPanel` the props `boys: Vec<(i64, String)>` and `on_boy_filter: EventHandler<Option<i64>>` and render `BoyChips { boys, selected: Some(user_id), allow_everyone: false, on_select: on_boy_filter }` above the picker/grid when `boys.len() > 1`; `School()` passes `enrollments.iter().filter(\|r\| r.enrolled).map(\|r\| (r.user_id, profile_name(clamp)))` and `move \|next\| boy_filter.set(next)`. Extend `hs5_h` and `hs5_i`: rendered with two enrolled boys the pane contains two chip buttons, exactly one `aria-pressed="true"`, and no `Everyone`. |
| QH1-06 | HS7 | S | `docs/VERIFICATION.md:1393`, `:1401` | Low | The Chrome-pass transcript quotes the gitignored TOML's `curriculum_name` string verbatim and lists six subject labels read off the loaded file. Not licensed schedule content (no week→reading mapping; the labels are the plan's own §1 vocabulary), but it is the only tracked file outside `.gitignore`/the plan wording that carries a string copied out of `ao-year-1.toml`, and N1's spirit is that the docs stay grep-clean too. | Replace the quoted name with `curriculum_name: "<the AO file's name>"` and the six labels with "the six *unshared* daily rows (Math first)". Nothing else in the transcript changes. |
| QH1-07 | HS5 | O | `src/client/components/homeschool/day_sheet.rs:84-88` | Low | `in_current_week == false` is rendered as "Not dealt out yet — finish week N first." for **every** date outside the span, including past ones — tapping last Tuesday in Month view tells the parent to finish a week that has already passed. H6's sentence is about future weeks only. | Add `before_span: bool` to `DaySheet` (from `School()`: `date < enrollment.week_started_on`); when `before_span` render "Before this week — only tasks you added are shown." and keep `not_dealt_out_line(week)` for the future case. Add a `hs5_j_*` SSR case for a past date asserting the past wording and no curriculum rows. |
| QH1-08 | HS1 | O | `src/server/homeschool/loader.rs:1086-1097` | Low | `import_curriculum` copies the file into `curricula_dir()` **before** `db::pool()` opens, and opening the pool runs `load_and_seed` over that same directory, so the command's own `insert_missing` always reports `0 subjects, 0 assignments, 0 term notes inserted (existing rows left untouched)` on a first import — verified here and in HS7's transcript. Correct on disk, misleading on the console. | Open the pool first: move `let pool = crate::server::db::pool().await…` above the `std::fs::copy`, so the boot loader runs against the directory as it was and the command's own report is truthful. `tests/service_tests.rs::import_curriculum_copies_a_valid_file_into_the_curricula_directory` gains `assert!(stdout.contains("7 subjects, 9 assignments, 3 term notes inserted"))` (the fixture's counts). |
| QH1-09 | HS3 | O | `docs/homeschool/PLAN_HOMESCHOOL.md:441` | Low | HS3 accept (e) names `cargo clippy --target wasm32-unknown-unknown --all-targets -- -D warnings`, which cannot compile on this tree (dev-dependency `tokio-tungstenite` → `mio` has no wasm backend; pre-existing, HANDOFF H-HS3-3). The gate that was actually run — and that passes here — is `cargo clippy --features web --target wasm32-unknown-unknown -- -D warnings`, the form `docs/BASELINE.md` uses. No code change is possible; the contract text is the defect. | Boss commit amending HS3 (e) to read `cargo clippy --features web --target wasm32-unknown-unknown -- -D warnings exits 0` and logging the amendment in `docs/HANDOFF.md` per PLAN §5.2. |
| QH1-10 | HS4 | S | `src/server/api/homeschool.rs:702-713`, `:912-919` | Low | `toggle_lesson` and `toggle_extra` are unauthenticated by contract, and `note: Option<String>` is written unbounded — any device on the LAN can persist arbitrarily long strings into `lesson_log.note` / `lesson_extras.note`. Not a contract clause, but the rest of the LAN-open surface caps its inputs (`title` ≤ 80 by CHECK, strokes by size). | At the top of both fns: `const MAX_NOTE_CHARS: usize = 500; if note.as_deref().is_some_and(\|n\| n.chars().count() > MAX_NOTE_CHARS) { return Err(validation_error("note is too long")); }`. One assertion in `hs4_d_*`: a 501-char note is rejected before any write. |

## Notes for the fixing wave

- QH1-01 and QH1-03 change server/shared behaviour the phone already assumes; land them first, then QH1-02/-04/-05 (client only).
- Nothing here touches `migrations/`, the DTOs, or a normative signature; every fix stays inside the owning task's files plus one new test each.
- Residuals recorded by the wave (`docs/RESIDUAL.md` R-2/R-3, HANDOFF H-HS3-5 load-flaky tests) stand; neither flaky test failed in either full run here.

## Boss close (2026-09-03)

| Item | Fixed on | Squash commit on `main` | Note |
| --- | --- | --- | --- |
| QH1-01 | `hs/HS4-qa1` | `082b2b3` | `hs4_n_*` added; `hs4_d_*` extended. |
| QH1-02 | `hs/HS5-qa1` | `b22e443` | memos read inside the resource closures; source-shape guard `hs5_qa1_*` in `glyph_tests`. |
| QH1-03 | `hs/HS3-qa1` | `71220b1` | one pre-existing `hs3_i` assertion (`due_today.is_empty()`) encoded the defect and now asserts `["Task 2"]`; logged in `docs/HANDOFF.md`. |
| QH1-04 | `hs/HS6-qa1` | `b5de105` | the audit's prescribed fixture ("every row `Some(Done)`" on the 9+3 day) leaves three ticked rows in `catch_up`, so the normative `celebrate` formula (`catch_up.is_empty() && …`) could not render the chip; `hs6_i` folds the catch-up rows into `due_today` before ticking (H3 rule 8 keeps only *unlogged* past work in `catch_up`). Formula unchanged; test name reads `…_and_a_boy_with_no_work_gets_no_school_today`. |
| QH1-05 | `hs/HS5-qa1` | `b22e443` | `BoyChips` in `today.rs`; Year and Month render it with `allow_everyone: false`. |
| QH1-06 | `hs/HS7-qa1` | `0b2a7b8` | docs-only. |
| QH1-07 | `hs/HS5-qa1` | `b22e443` | `DaySheet.before_span`, `BEFORE_SPAN_LINE`; `hs5_j_*` past-date case. |
| QH1-08 | `hs/HS1-qa1` @ `076367f` | **not merged** | BLOCKED — harness worktree-isolation refusal before the DONE baseline ran; see `docs/BLOCKED.md`. |
| QH1-09 | `hs/HS3-qa1` | `71220b1` | contract amendment logged in `docs/HANDOFF.md` per PLAN §5.2; the Boss close also restated (e)'s `chrono` half (H-HS3-4). |
| QH1-10 | `hs/HS4-qa1` | `082b2b3` | `MAX_NOTE_CHARS = 500` in both fns. |

Every merge was followed by a green baseline (fmt, clippy server `--all-targets`, clippy web
wasm, full `cargo test --features server`). Round 2 should re-audit QH1-01…-07, -09, -10 on
`main` and pick QH1-08 up when HS1-qa1 lands.
